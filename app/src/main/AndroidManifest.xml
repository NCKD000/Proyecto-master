<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.inacap.rca_apr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MemoriaJuegosActivity"></activity>
        <activity android:name=".EmocionalActivity" />
        <activity android:name=".ListaPacientesActivity" />
        <activity android:name=".DetallesPacientesActivity" />
        <activity android:name=".NuevoPacienteActivity" />
        <activity android:name=".MenuPacientesActivity" />
        <activity android:name=".MenuJuegosActivity" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>